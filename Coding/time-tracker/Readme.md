# Time Tracker

A lightweight focus timer that stores every deep-work session for the last 30 days directly in your browser.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

All session data is saved inside `localStorage` under the key `focus-tracker/v1`.
